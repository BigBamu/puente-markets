{
  "name": "puente-markets-mvp",
  "version": "0.1.0",
  "private": true,
  "description": "Puente Markets — private Polymarket simulator and market signal dashboard",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check app.js && node --check server.mjs && node --check api/markets.js && node --check api/orderbook.js"
  },
  "engines": {
    "node": ">=18"
  }
}
